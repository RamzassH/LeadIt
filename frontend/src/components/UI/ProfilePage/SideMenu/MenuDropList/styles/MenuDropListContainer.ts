"use client"
import {styled} from '@mui/material';

const MenuDropListContainer = styled("div")`
    width: 100%;
    display: flex;
    flex-direction: column;
    padding: 0;
`;

export default MenuDropListContainer;